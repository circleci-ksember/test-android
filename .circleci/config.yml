version: '2.1'
orbs:
  android: circleci/android@1.0.3
jobs:
  test:
    executor:
      name: android/android-machine
      resource-class: large
    steps:
      - checkout
      - run:
          command: |
            exit 4
      - run: echo this is after a failed
          when: on_fail
      
workflows:
  test:
    jobs:
      - test
