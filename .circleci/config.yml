version: '2.1'

jobs:
  test:
    docker:
      - image: cimg/android:2022.01
    steps:
      - checkout
      - name: pwd
        run: pwd
      - name: ls
        run: ls
      
workflows:
  test:
    jobs:
      - test
