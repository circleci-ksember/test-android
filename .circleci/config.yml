version: '2.1'

jobs:
  test:
    docker:
      - image: cimg/android:2022.01
    steps:
      - checkout
      - run: pwd
      - run: ls
      - run: echo $CIRCLE_WORKING_DIRECTORY
      
workflows:
  test:
    jobs:
      - test
