version: '2.1'

jobs:
  test:
    docker:
      - image: cimg/android:2022.01
    steps:
      - checkout
      - run: 
          command: |
            java -v
            ruby --version
            python3 --version
            echo $JAVA_HOME
      
workflows:
  test:
    jobs:
      - test
